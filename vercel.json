{
  "version": 2,
  "builds": [
    {
      "src": "jossie_fancies/wsgi.py",
      "use": "@vercel/python",
      "config": {
        "maxLambdaSize": "15mb"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "jossie_fancies/wsgi.py"
    }
  ],
  "installCommand": "pip install -r requirements.txt && npm install",
  "buildCommand": "npm run build-css-prod && mkdir -p staticfiles/css && cp static/css/output.css staticfiles/css/ && python manage.py collectstatic --noinput || true"
}